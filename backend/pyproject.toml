[project]
name = "autonomous-post-training-engineer"
version = "0.1.0"
description = "Backend for an observable ADK-driven autonomous post-training hackathon demo."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.133,<1",
  "httpx>=0.28,<1",
  "opentelemetry-api>=1.39,<2",
  "opentelemetry-sdk>=1.39,<2",
  "pydantic>=2.12,<3",
  "pydantic-settings>=2.10,<3",
  "python-dotenv>=1.1,<2",
  "uvicorn[standard]>=0.34,<1",
]

[project.optional-dependencies]
cloud = [
  "google-adk[a2a]>=2.2,<3",
  "google-cloud-aiplatform>=1.148,<2",
  "google-cloud-firestore>=2.21,<3",
  "google-cloud-secret-manager>=2.24,<3",
  "google-cloud-storage>=3.2,<4",
  "opentelemetry-exporter-gcp-trace>=1.9,<2",
]
dev = [
  "mypy>=1.17,<2",
  "pytest>=8.4,<9",
  "pytest-asyncio>=1.1,<2",
  "pytest-cov>=6.2,<7",
  "ruff>=0.12,<1",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q --strict-markers --disable-warnings"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC", "RUF"]

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
strict = true
warn_unused_ignores = true
